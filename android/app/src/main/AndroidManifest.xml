<!-- [android/app/src/main]/AndroidManifest.xml -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

<!-- Add permissions for internet and network state -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- CRITICAL: Boot and Alarm Permissions -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Alarm Permissions (Android 12+) -->
    <!-- USE_EXACT_ALARM: For alarm clock / calendar apps (no user permission needed) -->
    <!-- SCHEDULE_EXACT_ALARM: For other apps (requires user permission on Android 13+) -->
    <!-- Using USE_EXACT_ALARM since this is a reminder app -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" 
                     android:maxSdkVersion="32" />

    <!-- Alarm System Permissions (ENHANCED) -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Voice Recording Permissions -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MICROPHONE" />
    
    <!--Add permissions for Storage access (Android 10+) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
                     android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
                     android:maxSdkVersion="32" />

    <application
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:label="VoiceRemind">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:turnScreenOn="true"
            android:showWhenLocked="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    <!-- Required for notification action buttons -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ActionBroadcastReceiver" />

        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.NotificationBroadcastReceiver" />

        <!-- ============================================ -->
        <!-- CRITICAL: BOOT RECEIVER (UPDATED)           -->
        <!-- ============================================ -->
        <!-- This receiver triggers on device boot and reschedules ALL alarms -->
        <!-- MUST be exported=true to receive system broadcasts -->
        <receiver 
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="true">
            <intent-filter>
                <!-- Standard boot completed -->
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <!-- Direct Boot mode (before user unlocks device) -->
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <!-- Quick boot for some devices -->
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <!-- HTC devices -->
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- ============================================ -->
        <!-- NEW: ALARM BROADCAST RECEIVER                -->
        <!-- ============================================ -->
        <!-- This receiver handles alarm triggers from AlarmManager -->
        <receiver
            android:name=".AlarmBroadcastReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.voice_remind.ALARM_ACTION" />
            </intent-filter>
        </receiver>

        <!-- ============================================ -->
        <!-- REMOVE: BootWorker (NO LONGER NEEDED)       -->
        <!-- ============================================ -->
        <!-- The old WorkManager approach is replaced by direct AlarmManager rescheduling -->

        <!-- Alarm service for better coordination -->
        <service
            android:name="com.gdelataillade.alarm.services.AlarmService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Notification service for foreground alarms -->
        <service
            android:name="com.gdelataillade.alarm.services.NotificationOnKillService"
            android:exported="false" />

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>